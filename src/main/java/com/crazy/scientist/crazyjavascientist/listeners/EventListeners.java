package com.crazy.scientist.crazyjavascientist.listeners;


import net.dv8tion.jda.api.entities.Channel;
import net.dv8tion.jda.api.entities.Emoji;
import net.dv8tion.jda.api.entities.Emote;
import net.dv8tion.jda.api.entities.User;
import net.dv8tion.jda.api.events.message.react.MessageReactionAddEvent;
import net.dv8tion.jda.api.hooks.ListenerAdapter;
import org.jetbrains.annotations.NotNull;

public class EventListeners extends ListenerAdapter {


//    @Override
//    public void onMessageReactionAdd(@NotNull MessageReactionAddEvent event) {
//
//        User user = event.getUser();
//
//        String emoji = event.getReaction().getReactionEmote().getEmoji();
//
//        String channelMention = event.getChannel().getAsMention();
//        String jumpLink = event.getJumpUrl();
//
//        String responseMessage = user.getAsTag() + " reacted to a message with " + emoji + " in the "  + channelMention + " channel! Take a look here!\n" + jumpLink;
//        event.getGuild().getDefaultChannel().sendMessage(responseMessage).queue();
//
//
//
//
//    }


}
